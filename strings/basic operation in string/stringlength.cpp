#include <iostream>
#include <string>
using namespace std;

int main() {
    string str = "Hello pankaj";

    // Using size()
    cout << "Length using size(): " << str.size() << endl;

    // Using length()
    cout << "Length using length(): " << str.length() << endl;

    return 0;
}